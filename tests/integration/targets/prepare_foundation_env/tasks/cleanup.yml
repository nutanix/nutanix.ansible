---
- name: clean up the environment
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Delete files
      file:
        path: "{{ playbook_dir }}/tmp/integration-test-ntnx-package.tar.gz"
        state: absent
  